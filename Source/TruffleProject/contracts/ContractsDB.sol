contract ContractsDB {
    // construct, self destroy, etc

    function _add(bytes32 name, address addr) returns (bool result) {
        // permissions
    
        // store
    }


    function _remove(bytes32 name) returns (bool result) {
        // permissions

        // remove
    }
    
    function _get(bytes32 name) returns (address contractAddr) {
        // permissions

        // get
    }

}